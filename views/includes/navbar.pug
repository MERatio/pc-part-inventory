nav(class='navbar navbar-expand-lg navbar-dark bg-dark mb-3')
  div(class='container-fluid px-5')
    a(class='navbar-brand' href='/') Pc Part Inventory
    button(class='navbar-toggler' type='button' data-bs-toggle='collapse' data-bs-target='#navbarSupportedContent' aria-controls='navbarSupportedContent' aria-expanded='false' aria-label='Toggle navigation')
      span(class='navbar-toggler-icon')
    div(class='collapse navbar-collapse' id='navbarSupportedContent')
      ul(class='navbar-nav ms-auto mb-2 mb-lg-0')
        li(class='nav-item')
          if path === 'items'
            a(class='nav-link active' aria-current='page' href='/items') Items
          else
            a(class='nav-link' href='/items') Items
        li(class='nav-item')
          if path === 'categories'
            a(class='nav-link active' aria-current='page' href='/categories') Categories
          else
            a(class='nav-link' href='/categories') Categories
